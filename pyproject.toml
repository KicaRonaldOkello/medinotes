[project]
name = "medinotes"
version = "0.1.0"
description = "MediNotes FastAPI configuration for Vercel"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "fastapi-clerk-auth",
    "pydantic",
]

[project.scripts]
app = "api.server:app"

